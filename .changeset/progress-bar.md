---
"margo-ui": minor
---

ProgressBar component added, with `determinate` and `indeterminate` modes and a `--margo-progress-bar-fill` custom property to override the indicator
Spinner component added
